K8s Status Report v{{ .Chart.AppVersion }} - OpenLabs DevOps

Release "{{ .Release.Name }}" instalado no namespace "{{ include "status-report.namespace" . }}"

CronJob  : {{ include "status-report.fullname" . }}
Schedule : {{ .Values.cronjob.schedule }}
Cluster  : {{ .Values.config.clusters }}

Execucao manual:
  kubectl create job --from=cronjob/{{ include "status-report.fullname" . }} \
    {{ include "status-report.fullname" . }}-manual \
    -n {{ include "status-report.namespace" . }}

Logs:
  kubectl logs -l app.kubernetes.io/name={{ include "status-report.name" . }} \
    -n {{ include "status-report.namespace" . }} -f

OpenLabs DevOps | diego-f-santos@openlabs.com.br
